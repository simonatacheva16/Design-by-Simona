<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SIMONA | Premier Design Studio</title>

    <link href="https://fonts.googleapis.com/css2?family=Cinzel+Decorative:wght@700&family=Montserrat:wght@300;400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        :root {
            --primary-dark: #050505;
            --primary-bordox: #4d0000;
            --primary-gold: #D4AF37;
            --accent-gold-light: #fcf6ba;
            --text-light: #e0e0e0;
        }

        /* Основна структура - без скрити елементи */
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { 
            background: var(--primary-dark); 
            color: var(--text-light); 
            font-family: 'Montserrat', sans-serif; 
            line-height: 1.6;
        }

        /* Навигация */
        nav {
            position: fixed; top: 0; width: 100%; padding: 20px 5%;
            display: flex; justify-content: space-between; align-items: center;
            z-index: 1000; background: rgba(5, 5, 5, 0.95);
            border-bottom: 1px solid rgba(212, 175, 55, 0.3);
        }

        .brand-logo { 
            font-family: 'Cinzel Decorative', serif; 
            font-size: 1.8em; 
            color: var(--primary-gold); 
            letter-spacing: 3px; 
        }
        
        .nav-trigger { font-size: 1.5em; color: var(--primary-gold); cursor: pointer; }

        /* Меню */
        .full-screen-menu {
            position: fixed; top: 0; right: -100%; width: 100%; height: 100vh;
            background: var(--primary-bordox); z-index: 1001; 
            display: flex; flex-direction: column; justify-content: center; align-items: center;
            transition: 0.5s ease-in-out;
        }
        .full-screen-menu.active { right: 0; }
        .full-screen-menu a {
            color: white; text-decoration: none; font-size: 2.5em; 
            margin: 15px; font-family: 'Cinzel Decorative', serif;
        }

        /* Hero Section - Винаги видима */
        .hero {
            height: 80vh; 
            display: flex; flex-direction: column; justify-content: center; align-items: center; 
            text-align: center; 
            background: linear-gradient(rgba(0,0,0,0.6), rgba(0,0,0,0.6)), url('https://images.unsplash.com/photo-1549497538-3037991462b8?q=80&w=2070');
            background-size: cover; background-position: center;
        }

        .hero h1 { 
            font-family: 'Cinzel Decorative', serif; font-size: clamp(2.5em, 8vw, 5em); 
            color: var(--primary-gold); letter-spacing: 5px; margin-bottom: 10px;
        }

        .hero p { letter-spacing: 8px; font-size: 1.2em; color: var(--accent-gold-light); text-transform: uppercase; }

        /* Секции */
        .section { padding: 80px 5%; max-width: 1200px; margin: auto; }
        .section-header { text-align: center; margin-bottom: 50px; }
        .section-header h2 { font-size: 2.5em; color: var(--primary-gold); margin-bottom: 10px; }

        /* Карти с услуги */
        .service-grid { 
            display: grid; 
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); 
            gap: 30px; 
        }

        .premium-card {
            background: rgba(255, 255, 255, 0.05); 
            border: 1px solid rgba(212, 175, 55, 0.2); 
            padding: 40px 30px;
            text-align: center; 
            border-radius: 5px;
        }

        .premium-card i { font-size: 3em; color: var(--primary-gold); margin-bottom: 20px; display: block; }
        .premium-card h3 { font-size: 1.8em; margin-bottom: 15px; color: var(--accent-gold-light); }
        .price-tag { font-size: 2.5em; color: white; display: block; margin-bottom: 25px; }

        .global-btn {
            display: inline-block; padding: 12px 35px;
            background: transparent; border: 2px solid var(--primary-gold);
            color: var(--primary-gold); text-decoration: none; font-weight: bold;
            letter-spacing: 2px; text-transform: uppercase;
        }
        .global-btn:hover { background: var(--primary-gold); color: black; }

        footer { padding: 60px 5%; text-align: center; border-top: 1px solid rgba(212, 175, 55, 0.2); }
    </style>
</head>
<body>

    <nav>
        <div class="brand-logo">PREMIER DESIGN</div>
        <div class="nav-trigger" onclick="toggleMenu()"><i class="fas fa-bars"></i></div>
    </nav>

    <div class="full-screen-menu" id="fullMenu">
        <div style="position: absolute; top: 30px; right: 30px; font-size: 2.5em; color: var(--primary-gold); cursor: pointer;" onclick="toggleMenu()">&times;</div>
        <a href="#home" onclick="toggleMenu()">HOME</a>
        <a href="#services" onclick="toggleMenu()">SERVICES</a>
        <a href="mailto:simonatacheva16@gmail.com">CONTACT</a>
    </div>

    <section class="hero" id="home">
        <h1>Premier Design Studio</h1>
        <p>Style & Imagination</p>
    </section>

    <div class="section" id="services">
        <div class="section-header">
            <h2>Luxury Packages</h2>
            <div style="width: 60px; height: 2px; background: var(--primary-gold); margin: 20px auto;"></div>
        </div>

        <div class="service-grid">
            <div class="premium-card">
                <i class="fas fa-crown"></i>
                <h3>Signature Logo</h3>
                <p>3 concepts, vector formats, and full commercial rights.</p>
                <span class="price-tag">35<span>BGN</span></span>
                <a href="mailto:simonatacheva16@gmail.com?subject=Inquiry: Logo" class="global-btn">Start</a>
            </div>

            <div class="premium-card">
                <i class="fas fa-gem"></i>
                <h3>Branding Identity</h3>
                <p>Logo, business cards, and professional brand guide.</p>
                <span class="price-tag">80<span>BGN</span></span>
                <a href="mailto:simonatacheva16@gmail.com?subject=Inquiry: Branding" class="global-btn">Order</a>
            </div>

            <div class="premium-card">
                <i class="fas fa-magic"></i>
                <h3>Digital Store</h3>
                <p>Social media kits, planners, and custom assets.</p>
                <span class="price-tag">25<span>BGN</span></span>
                <a href="mailto:simonatacheva16@gmail.com?subject=Inquiry: Digital" class="global-btn">Explore</a>
            </div>
        </div>
    </div>

    <footer>
        <h2 style="font-family: 'Cinzel Decorative'; margin-bottom: 20px;">Let's Create Magic</h2>
        <a href="mailto:simonatacheva16@gmail.com" style="font-size: 1.2em;">simonatacheva16@gmail.com</a>
        <p style="margin-top: 30px; opacity: 0.4;">&copy; 2025 PREMIER DESIGN STUDIO. ALL RIGHTS RESERVED.</p>
    </footer>

    <script>
        function toggleMenu() {
            document.getElementById('fullMenu').classList.toggle('active');
        }
    </script>
</body>
</html>